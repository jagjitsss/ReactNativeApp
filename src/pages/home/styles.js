import { StyleSheet } from 'react-native';
import { HomeStyle } from './AppStyles';

const styles = StyleSheet.create({
  container: HomeStyle.container,
  photo: HomeStyle.photo,
  title: HomeStyle.title,
  category: HomeStyle.category,
  headingcontainer:HomeStyle.headingcontainer,
  headingings:HomeStyle.headingings,
  topHeading:HomeStyle.topHeading,
  fulltitle:HomeStyle.fulltitle,
  CategoryContainer:HomeStyle.CategoryContainer,
  Promocontainer:HomeStyle.Promocontainer,
  promImg:HomeStyle.promImg
});

export default styles;
