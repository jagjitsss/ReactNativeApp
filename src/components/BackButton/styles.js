import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
 
  btnIcon: {
    height: 30,
    width: 30,
    marginLeft:2,
    marginTop:15,
    color:"#fff"
  },

 
});

export default styles;
